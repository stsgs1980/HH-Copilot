{
  "name": "hh-auto-respond-extension",
  "version": "1.6.0",
  "private": true,
  "description": "HH.ru Auto-Respond Chrome Extension — esbuild bundled content script",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:prod": "node esbuild.config.mjs --production",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

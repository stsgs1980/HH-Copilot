{
  "name": "hh-copilot",
  "version": "1.9.86.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.9",
    "@vitest/coverage-v8": "^4.1.9",
    "@vitest/ui": "^4.1.9",
    "esbuild": "^0.24.0",
    "eslint": "^10.5.0",
    "globals": "^17.6.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.9",
    "ws": "^8.18.0"
  },
  "description": "HH Copilot — Chrome Extension for hh.ru (esbuild)",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:prod": "node esbuild.config.mjs --production",
    "watch": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ background/ tests/",
    "lint:fix": "eslint --fix src/ background/ tests/",
    "lint:ci": "eslint src/ background/ tests/",
    "cascade": "node ../scripts/cascade-task.js"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
}
